@page "/Teams"
@using Microsoft.AspNetCore.Authorization
@using EM.Infrastructure.Data
@attribute [Authorize(Roles = $"{IdentityConstants.TEAMS_VIEW_ROLE_NAME}")]

<AuthorizeView>
    <Authorized>
        <h3>Teams</h3>
    </Authorized>
</AuthorizeView>

@code {
    
}